services:
  - type: web
    name: dealsniper-server
    env: docker
    region: ohio
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
